MODEL=unsloth/Mistral-Small-24B-Instruct-2501
API_KEY=
LLM_BASE=https://llm.chutes.ai/v1

X_BEARER_TOKEN=

X_API_BASE=https://api.twitter.com/2

MINER_API_URL=https://talisman.rizzo.network/api
BATCH_HTTP_TIMEOUT=30.0

MAX_POSTS=0

# Scraping configuration
# Interval between scrape cycles in seconds (900 = 15 minutes, yields ~18 posts per 4.5 hour batch)
SCRAPE_INTERVAL_SECONDS=900
# Maximum number of tweets to fetch per API call (default: 10)
# Lower values reduce API usage; higher values provide more buffer
MAX_RESULTS=10
# Number of posts to scrape per cycle
POSTS_PER_SCRAPE=3
# Number of posts to submit per cycle (should be <= POSTS_PER_SCRAPE)
POSTS_TO_SUBMIT=1
